
// Componente de boton de siguiente cita

const Buttons = ({handle}) => {

    return(


            <button onClick={handle}>
                New Quote
            </button>



    )
}
export default Buttons;
