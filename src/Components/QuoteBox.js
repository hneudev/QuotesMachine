
// Componente de boton de campo de texto para la cita

const QuoteBox = ({quote}) => {
  return(



             <div className="quote">
                 <p>
                     {quote.quote}
                 </p>

                 
              </div>

  )
}
export default QuoteBox;